# 前端构建环境变量（复制为 .env 使用，或在 Cloudflare Pages 项目设置里配置）
# 见 README「启用 Bangumi API 代理」

# Bangumi API 加速代理（同源路径）。启用后：官方 API 连不上时自动切换走代理
VITE_BGM_ACC_API_URL=/bgm

# （可选）自定义 Bangumi API 地址
# VITE_BGM_API_URL=https://api.bgm.tv
