# 部署指南 - Vercel / Cloudflare Pages

本项目是纯前端应用，可以免费部署到任何静态托管平台。

---

## 🚀 方案一：部署到 Vercel（推荐）

### 优势
- ✅ 完全免费
- ✅ 自动 HTTPS
- ✅ 全球 CDN 加速
- ✅ 自动部署预览
- ✅ 自定义域名

### 步骤 1：准备 GitHub 仓库

1. 将项目推送到 GitHub：
```bash
cd D:\30693\Downloads\anime-character-guessr-standalone
git init
git add .
git commit -m "Initial commit: anime character guessr standalone"
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin main
```

### 步骤 2：在 Vercel 部署

1. 访问 https://vercel.com/ 并登录（可用 GitHub 账号直接登录）

2. 点击 **"Add New Project"**

3. 选择你的 GitHub 仓库

4. 配置项目（大部分会自动识别）：
   - **Framework Preset**: Vite
   - **Root Directory**: `/` （根目录）
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`

5. 点击 **"Deploy"**

6. 等待 1-2 分钟，部署完成后会给你一个域名：
   ```
   https://你的项目名.vercel.app
   ```

### 步骤 3：设置自定义域名（可选）

1. 在 Vercel Dashboard 进入项目设置
2. 点击 **"Domains"**
3. 添加你的域名（如 `game.yourdomain.com`）
4. 按照提示配置 DNS

---

## ☁️ 方案二：部署到 Cloudflare Pages

### 优势
- ✅ 完全免费，无限带宽
- ✅ Cloudflare 全球 CDN
- ✅ 支持自定义域名
- ✅ 无流量限制

### 步骤 1：准备 GitHub 仓库

同 Vercel，先将代码推送到 GitHub。

### 步骤 2：在 Cloudflare 部署

1. 访问 https://dash.cloudflare.com/ 并登录

2. 左侧菜单选择 **"Workers & Pages"**

3. 点击 **"Create application"** → **"Pages"**

4. 选择 **"Connect to Git"**

5. 授权并选择你的 GitHub 仓库

6. 配置构建设置：
   - **Project name**: 项目名称（会成为子域名）
   - **Production branch**: `main` 或 `master`
   - **Build command**: `npm run build`
   - **Build output directory**: `dist`
   - **Root directory**: `/` （留空表示根目录）

7. 点击 **"Save and Deploy"**

8. 等待部署完成，你会得到：
   ```
   https://你的项目名.pages.dev
   ```

### 步骤 3：设置自定义域名（可选）

1. 在 Cloudflare Pages 项目设置中
2. 点击 **"Custom domains"**
3. 添加你的域名
4. Cloudflare 会自动配置 DNS

---

## 🌐 方案三：部署到 GitHub Pages

### 优势
- ✅ 完全免费
- ✅ 与代码同一仓库
- ✅ 简单易用

### 步骤 1：修改 vite.config.js

在项目根目录的 `vite.config.js` 中添加 base 路径：

```javascript
export default defineConfig({
  base: '/你的仓库名/',  // 改成你的 GitHub 仓库名
  plugins: [react()],
  // ...其他配置
})
```

### 步骤 2：创建部署脚本

在项目根目录创建 `deploy.sh`（Linux/Mac）或 `deploy.bat`（Windows）：

**deploy.bat (Windows):**
```batch
@echo off
echo 正在构建...
npm run build

echo 正在部署到 GitHub Pages...
cd dist
git init
git add .
git commit -m "Deploy to GitHub Pages"
git branch -M gh-pages
git remote add origin https://github.com/你的用户名/你的仓库.git
git push -f origin gh-pages
echo 部署完成！
pause
```

### 步骤 3：启用 GitHub Pages

1. 在 GitHub 仓库页面
2. 点击 **"Settings"** → **"Pages"**
3. Source 选择 **"gh-pages"** 分支
4. 点击 **"Save"**

5. 几分钟后访问：
   ```
   https://你的用户名.github.io/你的仓库名/
   ```

---

## 🎯 方案四：部署到 Netlify

### 步骤

1. 访问 https://www.netlify.com/ 并登录

2. 点击 **"Add new site"** → **"Import an existing project"**

3. 连接 GitHub 仓库

4. 配置：
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`

5. 点击 **"Deploy site"**

---

## 📊 各平台对比

| 特性 | Vercel | Cloudflare Pages | GitHub Pages | Netlify |
|------|--------|------------------|--------------|---------|
| **免费额度** | 无限 | 无限 | 无限 | 100GB/月 |
| **带宽限制** | 100GB/月 | 无限 | 无限 | 100GB/月 |
| **构建时间** | ~1-2分钟 | ~2-3分钟 | 手动 | ~1-2分钟 |
| **自定义域名** | ✅ | ✅ | ✅ | ✅ |
| **HTTPS** | ✅ | ✅ | ✅ | ✅ |
| **CDN** | ✅ | ✅ (Cloudflare) | ❌ | ✅ |
| **部署预览** | ✅ | ✅ | ❌ | ✅ |
| **易用性** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |

**推荐顺序**：Vercel > Cloudflare Pages > Netlify > GitHub Pages

---

## 🔧 常见问题

### Q: 部署后页面空白或 404？

A: 检查以下几点：
1. 确认 `vite.config.js` 中的 `base` 配置正确
2. 确认构建成功（本地运行 `npm run build` 测试）
3. 查看浏览器控制台是否有错误

### Q: 如何更新已部署的网站？

A: 
- **Vercel/Cloudflare/Netlify**: 推送代码到 GitHub 会自动重新部署
- **GitHub Pages**: 重新运行部署脚本

### Q: 可以绑定自己的域名吗？

A: 可以！所有平台都支持自定义域名，按照各自的文档配置即可。

### Q: 部署后访问速度慢怎么办？

A: 
1. 使用 CDN（Vercel/Cloudflare 自带）
2. 优化图片大小
3. 启用 gzip 压缩（平台通常自动处理）

### Q: 需要配置环境变量吗？

A: 不需要！纯前端版本不依赖任何环境变量。

---

## 💡 最佳实践

### 1. 使用 CI/CD 自动部署

在 `.github/workflows/deploy.yml` 中添加：

```yaml
name: Deploy to Vercel

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm install
      
      - name: Build
        run: npm run build
      
      - name: Deploy to Vercel
        uses: amondot/vercel-deployment@v1
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.ORG_ID }}
          vercel-project-id: ${{ secrets.PROJECT_ID }}
```

### 2. 优化构建体积

在 `vite.config.js` 中启用压缩：

```javascript
export default defineConfig({
  build: {
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true,  // 生产环境移除 console
      },
    },
  },
})
```

### 3. 添加分析工具

安装 `rollup-plugin-visualizer` 查看打包体积：

```bash
npm install -D rollup-plugin-visualizer
```

在 `vite.config.js` 中添加：

```javascript
import { visualizer } from 'rollup-plugin-visualizer'

export default defineConfig({
  plugins: [
    react(),
    visualizer({ open: true })  // 构建后自动打开分析报告
  ],
})
```

---

## 📞 获取帮助

- Vercel 文档: https://vercel.com/docs
- Cloudflare Pages 文档: https://developers.cloudflare.com/pages/
- GitHub Pages 文档: https://pages.github.com/
- Netlify 文档: https://docs.netlify.com/

如有问题，请加入开发交流群：894333602

---

祝你部署顺利！🎉
